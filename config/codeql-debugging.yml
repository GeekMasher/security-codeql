name: "GeekMasher CodeQL Java Debugging Configuration"

disable-default-queries: true

queries:
  - uses: config/suite-debugging-java.qls
  # >> Sources
  - uses: java/debugging/Sources.ql
  - uses: csharp/debugging/Sources.ql

  
paths:
  - '/**/'
