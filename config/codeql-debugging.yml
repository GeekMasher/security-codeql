name: "GeekMasher CodeQL Java Debugging Configuration"

disable-default-queries: true

queries:
  - uses: GeekMasher/security-queries/config/suite-debugging-java.qls@main
  # >> Sources
  - uses: GeekMasher/security-queries/java/debugging/Sources.ql@main
  - uses: GeekMasher/security-queries/csharp/debugging/Sources.ql@main

paths:
  - '/**/'
