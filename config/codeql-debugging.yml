name: "GeekMasher CodeQL Java Debugging Configuration"

disable-default-queries: true

queries:
  - uses: GeekMasher/security-queries/config/suite-debugging-java.qls
  # >> Sources
  - uses: GeekMasher/security-queries/java/debugging/Sources.ql
  - uses: GeekMasher/security-queries/csharp/debugging/Sources.ql

paths:
  - '/**/'
